import Particles from 'react-tsparticles';
import { particlesConfigDay } from './particlesDayConfig';

const ParticlesBackgroundDay = () => {
  return <Particles params={particlesConfigDay}></Particles>;
};

export default ParticlesBackgroundDay;
